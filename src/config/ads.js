export const ADSENSE_CLIENT = 'ca-pub-3697813092001678'

// Create ad units in Google AdSense → Ad units → "+" and paste the
// numeric slot ID here (e.g. "1234567890"). Until a real ID is set,
// the ad is hidden and you can still use Auto Ads from the dashboard.
export const AD_SLOTS = {
  primary: 'REPLACE_WITH_PRIMARY_AD_SLOT_ID',
  secondary: 'REPLACE_WITH_SECONDARY_AD_SLOT_ID',
}